---
layout: base
---

%h1
  Download

%h2
  Manual Download
  
%p
  Click the link below to download the latest release of Togglz. The ZIP file contains the Togglz core modules and all
  the optional integration modules.

%p.calign
  %a(href='/downloads/togglz-#{site.stableVersion}.zip' class='biglink')
    togglz-#{site.stableVersion}.zip

%p
  If you want to give the latest snapshots a try, grab it here:

%p.calign
  %a(href='https://oss.sonatype.org/content/repositories/snapshots/org/togglz/togglz-distribution/#{site.developmentVersion}/' class='biglink')
    togglz-#{site.developmentVersion}.zip

%h2
  Apache Maven

%p  
  The Togglz artifacts are available in the Maven Central repository. So you don't have to add any additional
  repositories to your pom.xml file. 
  
%p
  Togglz has been developed in a very modular manner. This allows you to select exactly the components of the 
  framework that you want to use. The Togglz Servlet module is the only required one:

%pre(class="prettyprint lang-xml")
  :escaped
    <!-- Togglz for Servlet environments (mandatory) -->
    <dependency>
      <groupId>org.togglz</groupId>
      <artifactId>togglz-servlet</artifactId>
      <version>#{site.stableVersion}</version>
    </dependency>

    <!-- Additional modules (optional) -->
    <dependency>
      <groupId>org.togglz</groupId>
      <artifactId>togglz-{some-module}</artifactId>
      <version>#{site.stableVersion}</version>
    </dependency>

%p
  Which of the optional modules to add depends on your specific environment. You can find the full list
  of modules available in Maven central here:

%p.calign
  %a(href='http://search.maven.org/#search%7Cga%7C1%7Cg%3A%22org.togglz%22')
    Togglz modules on Maven Central

%p
  If you want to give the latest snapshots a try, you will have to add the Sonatype OSS snapshot 
  repository to your <code>pom.xml</code> and set the Togglz version to <code>#{site.developmentVersion}</code>:

%pre(class="prettyprint lang-xml")
  :escaped
    <dependencies>
      
      <dependency>
        <groupId>org.togglz</groupId>
        <artifactId>togglz-servlet</artifactId>
        <version>#{site.developmentVersion}</version>
      </dependency>

      <!-- more dependencies -->      
      
    </dependencies>

    <repositories>
      <repository>
        <id>sonatype-oss-snapshots</id>
        <url>https://oss.sonatype.org/content/repositories/snapshots</url>
        <releases>
          <enabled>false</enabled>
        </releases>
        <snapshots>
          <enabled>true</enabled>
        </snapshots>
      </repository>
    </repositories>
    


